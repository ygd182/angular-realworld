import { BaseClassDirective } from './base-class.directive';

describe('BaseClassDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseClassDirective();
    expect(directive).toBeTruthy();
  });
});
