import { ShowLoggedInDirective } from './show-logged-in.directive';

describe('ShowLoggedInDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowLoggedInDirective();
    expect(directive).toBeTruthy();
  });
});
